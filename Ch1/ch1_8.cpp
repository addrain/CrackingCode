/**
 * Adrian Kwok
 *
 * Zero Matrix: Write an algorithm such that if an element in 
 * an MxN matrix is 0, its entire row and column are set to 0.
 */

#include <iostream>

using namespace std;

int main(){
    int m = 3;
    int n = 4;
    int matrix[m][n];
    for (int i = 0; i < m; i++){
        for(int j = 0; j < n; j++){
            if(matrix[i][j] == 0){
                //store i and j in an array
            }
        }
    }
    //loop for replacing column by using indeces stored in array for i and st 
}